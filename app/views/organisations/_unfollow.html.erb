 	<% if user_signed_in? then %>
<%= form_for(current_user.relationships.find_by_organisation_id(@organisation),
             html: { method: :delete },
             remote: true, class: 'follow') do |f| %>
  <%= f.submit "Unfollow", class: "btn-unfollow" %>
<% end %><% end %>